#include <luminis/core/photon.hpp>

namespace luminis::core {

// Photon::Photon(std::array<double, 3> pos, std::array<double, 3> dir, double
// wl)
//     : position(pos), direction(dir), wavelength(wl) {}

// void Photon::move(double distance) {
//   for (int i = 0; i < 3; i++) {
//     position[i] += direction[i] * distance;
//   }
// }

} // namespace luminis::core
